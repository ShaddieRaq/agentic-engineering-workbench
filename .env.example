# Required for model-invoking stages (interview, architect, capability,
# test design, improvement analysis). The console boots without it, but
# every stage run returns 503 until it is set.
OPENAI_API_KEY=replace_with_your_api_key

# Optional: web console port (default 4173).
# PORT=4173
