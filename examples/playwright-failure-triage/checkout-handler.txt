Application handler and observed server evidence:

export async function checkout(request) {
  const inventory = await inventoryClient.reserve(request.cartId);
  if (!inventory.ok) {
    throw new Error("Inventory service unavailable");
  }
  return { status: 200 };
}

Observed application log for the failed request:
InventoryClientError: connection pool exhausted
at checkout (src/api/checkoutHandler.ts:18:11)
